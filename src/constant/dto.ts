export interface LoginDTO {
    email: string;
    password : string;
}

export interface SignupDTO {
    firstname : string;
    lastname : string;
    email : string;
    password : string;
}

export interface collectionDTO {
    name: string;
    ids : any;
}